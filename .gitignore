# Python
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Datasets (too large for git, download via scripts/download_dataset.py)
dataset/
*.zip

# Trained weights (large binaries — publish via GitHub Releases instead)
*.pt
!yolo11n.pt.placeholder
# Exception: the Phase-2 deployment ships its serving weights in-repo, as the
# assignment requires the model weights to be included with the submission.
!deployment/models/best.pt
runs/

# OS/editor cruft
.DS_Store
.vscode/
*.log

# Environment secrets
.env
.env.local
.env.*.local
